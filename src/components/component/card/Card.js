import React from "react";
import Text from "../text/Text";

export default function Card({ title, link }) {
    return (

        <div>
            <Text
                text={title}
            />
        </div>
        
    )
}
